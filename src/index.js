const binary = document.querySelector("form input #binary-input");
const decimalInput = document.querySelector("form input #decimal-input");

const binaryValue = binary.value;

function tranformToDecimal(binaryValue) {

}

binaryValue.addEventListener('click', tranformToDecimal(binaryValue))